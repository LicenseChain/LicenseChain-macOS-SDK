# LicenseChain macOS SDK Roadmap

## Current Focus

- Keep API base URL behavior aligned with `https://api.licensechain.app/v1`.
- Maintain parity with iOS verification and JWT/JWKS helper behavior.
- Keep macOS integration docs aligned with current API contract.

## Next Operational Tasks

- Re-run conformance checks after Core API verify/JWKS updates.
- Keep docs and examples synchronized with ecosystem references.
- Capture notable updates in `CHANGELOG.md`.
